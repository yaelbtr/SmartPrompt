import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yaelbtr.smartprompt',
  appName: 'smartPrompt',
  webDir: 'dist/smartPrompt/browser'
};

export default config;
